name: Build app and webapp

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute gradle build
        run: cd riac-webapp && ./gradlew build
